import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["img.freepik.com"], // Add your external image domain here
  },
  // other config options here
};

export default nextConfig;
